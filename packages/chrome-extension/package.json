{
  "name": "@chrome-agent-bridge/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc && cp manifest.json src/popup.html src/popup.css dist/ && cp -r src/images dist/images && sed -i '' 's/^export //g' dist/content.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "@chrome-agent-bridge/shared": "workspace:*"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/chrome": "^0.1.37"
  }
}
