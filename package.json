{
  "name": "chrome-agent-bridge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "fast-check": "^4.1.1",
    "jsdom": "^28.1.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.1"
  }
}
